import userApi from '../../api/user';

const state = {
    tipInfo:{
    }
}

// getters
const getters = {
}

// actions
const actions = {
}

// mutations
const mutations = {
}

export default {
    state,
    getters,
    mutations,
    actions
}